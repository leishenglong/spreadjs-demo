# 财务数据收集系统 - 使用指南

## 📋 系统概述

本系统基于 SpreadJS 构建，支持复杂的财务数据收集、多级汇总和跨表计算流程。

## 🎯 典型业务场景

```
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│  员工/部门填报   │ -> │   数据汇总表    │ -> │   中间计算表    │ -> │   集团合并报表   │
│  (基础数据)     │    │  (按组织汇总)   │    │  (公式计算)     │    │  (最终报表)     │
└─────────────────┘    └─────────────────┘    └─────────────────┘    └─────────────────┘
```

### 工作簿结构示例

```
工作簿
├── Sheet1: 基础数据填报
│   ├── 员工信息表
│   ├── 费用明细表
│   └── 预算申请表
│
├── Sheet2: 部门汇总
│   ├── 按科目汇总
│   └── 按人员汇总
│
├── Sheet3: 中间计算
│   ├── 成本分摊计算
│   └── 绩效指标计算
│
└── Sheet4: 集团报表
    ├── 月度利润表
    └── 现金流量表
```

## 🚀 快速开始

### 1. 登录系统

| 角色 | 用户名 | 密码 | 权限 |
|------|--------|------|------|
| 管理员 | admin | admin123 | 模板设计、流程管理、全部数据 |
| 普通用户 | user1 | user123 | 填报任务、查看自己的数据 |

### 2. 创建填报模板

#### 方式一：使用预设模板

系统已内置以下财务模板：

| 模板名称 | 适用场景 | 包含字段 |
|---------|---------|---------|
| 差旅费报销单 | 员工出差费用报销 | 姓名、部门、交通费、住宿费、餐费、小计 |
| 日常费用报销单 | 办公费用报销 | 姓名、部门、费用项目、金额、日期 |
| 部门月度预算表 | 部门预算申报与执行 | 部门、月份、预算总额、已用金额、使用率 |
| 收入明细表 | 业务收入登记 | 收入项目、收入金额、收入类型、确认日期 |
| 费用汇总表 | 按科目汇总 | 科目、本月发生、本年累计、预算、完成率 |
| 基础数据采集表 | 通用数据采集 | 数据编号、名称、值、单位、填报人 |

#### 方式二：自定义模板

1. 进入 **模板管理** -> **新建模板**
2. 点击右侧预设字段快速添加：
   - **基础信息**: 姓名、工号、部门、岗位、日期
   - **费用报销**: 费用类型、金额、日期、说明
   - **差旅费用**: 出发地、目的地、交通/住宿/餐费、小计
   - **日常费用**: 费用项目、金额、日期、备注
   - **预算管理**: 预算项目、预算金额、已用金额、剩余金额
   - **月度汇总**: 科目、本月发生、本年累计、预算、完成率

### 3. 设置数据绑定

#### 单元格绑定（单字段绑定）

```
步骤：
1. 在表格中选中一个单元格（如 A1）
2. 在右侧字段列表中点击字段名按钮
3. 单元格即绑定到该字段
```

**数据格式：**
```javascript
{
  name: '张三',
  amount: 1000,
  date: '2024-01-15'
}
```

#### 表格绑定（批量字段绑定）

```
步骤：
1. 配置多个字段（每个都有 bindingPath）
2. 在表格中选中起始单元格
3. 点击 "📊 创建表格绑定"
```

**数据格式：**
```javascript
{
  records: [
    { name: '张三', amount: 1000, date: '2024-01-15' },
    { name: '李四', amount: 2000, date: '2024-01-16' },
    { name: '王五', amount: 1500, date: '2024-01-17' }
  ]
}
```

### 4. 创建填报流程

1. 进入 **流程管理** -> **创建流程**
2. 配置流程步骤：
   - 步骤1：填写（全体员工）
   - 步骤2：部门审批（部门经理）
   - 步骤3：财务审核（财务）
3. 关联模板并保存

### 5. 下发填报任务

管理员可以：
- 为特定用户创建填报任务
- 设置截止日期
- 追踪填报进度

### 6. 用户填报

1. 用户登录后查看 **我的任务**
2. 点击 **填写** 进入填报页面
3. 填写数据后：
   - **保存草稿**: 暂存，可继续编辑
   - **提交**: 完成填报，进入下一流程

### 7. 查看结果

- **任务列表**: 查看所有任务状态
- **查看详情**: 查看已提交的数据和流程记录

## 💡 高级功能

### 公式字段

在 Designer 中可以设置公式字段：

```
示例1：行内计算
字段：小计
公式：=C3+D3+E3+F3
说明：计算同一行的交通费+住宿费+餐费+其他费用

示例2：使用表格结构化引用
字段：总计
公式：=SUM(明细表[报销金额])
说明：汇总名为"明细表"的表格中的"报销金额"列

示例3：条件判断
字段：完成率
公式：=IF(E2>0,D2/E2,0)
说明：如果预算>0，则计算已用/预算，否则为0
```

### 跨 Sheet 计算

SpreadJS 支持跨工作表引用：

```javascript
// Sheet1 中的公式
// 引用 Sheet2 的数据
'=Sheet2!A1 + Sheet2!B1'

// 汇总多个 Sheet 的数据
'=SUM(Sheet1:Sheet5!D:D)'
```

### 数据验证

在 Designer 中可以设置单元格验证规则：

1. 选中单元格
2. 右键 -> 数据验证
3. 设置验证条件（如：只能输入数字、日期范围等）

## 📊 财务场景示例

### 场景1：月度费用报销汇总

**第一步**：员工填报（Sheet1 - 基础数据）
```
姓名    部门    费用类型    金额    日期
张三    销售部   差旅费    1000   2024-01-15
李四    财务部   办公费    500    2024-01-16
```

**第二步**：部门汇总（Sheet2 - 汇总表）
```
部门    本月合计    本年累计    预算    完成率
销售部   50000      500000     600000  83%
财务部   20000      200000     180000  111%
```

**第三步**：集团合并（Sheet3 - 集团报表）
```
科目    销售部    财务部    人力部    合计
差旅费   30000    5000      10000    45000
办公费   10000    8000      5000     23000
...
总计     50000    20000     30000    100000
```

### 场景2：预算控制流程

1. **预算申报**（月初）
   - 部门填报月度预算
   - 财务审核预算

2. **费用录入**（月度进行中）
   - 员工报销时自动扣减预算
   - 实时显示剩余预算

3. **预算分析**（月末）
   - 自动计算预算执行率
   - 生成预算执行分析表

## 🔧 技术说明

### 数据绑定 API

```javascript
// 设置单元格绑定路径
sheet.setBindingPath(row, col, "fieldName")

// 设置表格绑定
table.bindingPath("records")
table.bindColumns([
  { name: "姓名", dataField: "name" },
  { name: "金额", dataField: "amount" }
])

// 设置数据源
const dataSource = new GC.Spread.Sheets.Bindings.CellBindingSource({
  records: [
    { name: "张三", amount: 1000 }
  ]
})
sheet.setDataSource(dataSource)
```

### 公式计算

```javascript
// 设置单元格公式
sheet.setFormula(row, col, "=A1+B1")

// 设置表格列公式（数据行）
table.setColumnDataFormula(colIndex, "=[金额]*1.1")

// 设置表格列公式（汇总行）
table.setColumnFormula(colIndex, "=SUBTOTAL(109,[金额])")
```

## 📝 注意事项

1. **绑定路径必须唯一**: 同一数据源中，bindingPath 不能重复
2. **公式字段自动计算**: 提交时会自动计算公式结果
3. **SSJSON 包含完整信息**: 模板保存后包含所有样式、公式、绑定信息
4. **跨表引用需要**: 被引用的 Sheet 必须存在于同一工作簿中

## 🎓 最佳实践

1. **使用表格绑定**：对于结构化数据，优先使用表格绑定
2. **预设字段复用**：使用系统预设字段模板提高效率
3. **公式使用结构化引用**：如 `=SUM(明细表[金额])` 而非 `=SUM(C2:C100)`
4. **分步验证**：每完成一个 Sheet 的设计后先测试数据绑定
5. **版本管理**：重要修改前建议复制一份作为备份
